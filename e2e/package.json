{
  "name": "@rtc-less/e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test --reporter=list",
    "test:json": "playwright test --reporter=json"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
